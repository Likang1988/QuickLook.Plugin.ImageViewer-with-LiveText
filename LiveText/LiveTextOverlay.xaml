<UserControl x:Class="QuickLook.Plugin.ImageViewer.LiveText.LiveTextOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent"
             IsHitTestVisible="True"
             Cursor="Cross">
    
    <UserControl.Resources>
        <!-- 文本边界框样式 -->
        <Style x:Key="TextBoundStyle" TargetType="Rectangle">
            <Setter Property="Stroke" Value="LightBlue" />
            <Setter Property="StrokeThickness" Value="1" />
            <Setter Property="Fill" Value="Transparent" />
            <Setter Property="Opacity" Value="0.8" />
        </Style>
        

        
        <!-- 选择框样式 -->
        <Style x:Key="SelectionBoxStyle" TargetType="Rectangle">
            <Setter Property="Fill" Value="#300078D4" />
            <Setter Property="Stroke" Value="DodgerBlue" />
            <Setter Property="StrokeThickness" Value="1" />
            <Setter Property="StrokeDashArray" Value="3,2" />
            <Setter Property="Opacity" Value="0.6" />
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <!-- 遮罩层 -->
        <Path x:Name="MaskPath"
              Fill="#80000000"
              Visibility="Visible">
            <Path.Data>
                <GeometryGroup x:Name="MaskGeometry" FillRule="EvenOdd">
                    <!-- 外层矩形覆盖整个区域 -->
                    <RectangleGeometry x:Name="OuterRect" />
                    <!-- 文本区域的洞将在代码中动态添加 -->
                </GeometryGroup>
            </Path.Data>
        </Path>
        
        <!-- 主画布，用于绘制文本边界框和选择区域 -->
        <Canvas x:Name="MainCanvas" 
                Background="Transparent"
                ClipToBounds="True">
            
            <!-- 文本边界框将在代码中动态添加到这里 -->
            
        </Canvas>
        
        <!-- 状态指示器 -->
        <Border x:Name="StatusIndicator"
                Background="#CC000000"
                CornerRadius="4"
                Padding="8,4"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="10"
                Visibility="Collapsed">
            <TextBlock x:Name="StatusText"
                       Foreground="White"
                       FontSize="12"
                       Text="正在识别文本..." />
        </Border>
        
        <!-- 帮助提示 -->
        <Border x:Name="HelpTip"
                Background="#CC000000"
                CornerRadius="4"
                Padding="12,6"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="0,0,0,20"
                Visibility="Collapsed">
            <StackPanel Orientation="Vertical">
                <TextBlock Foreground="White"
                           FontSize="11"
                           Text="拖拽选择文本区域，右键复制" 
                           HorizontalAlignment="Center" />
                <TextBlock Foreground="#CCCCCC"
                           FontSize="10"
                           Text="Ctrl+A 全选 • Ctrl+C 复制 • Esc 退出"
                           HorizontalAlignment="Center"
                           Margin="0,2,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>